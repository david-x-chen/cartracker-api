build:
  version: 2018-03-15 # version of the build schema.
  steps:
    builder:
      name: builder
      dockerfile: Dockerfile.builder
      artifacts:
        - /usr/local/go/src/cartracker.api/main
    production:
      name: helloworld:latest
      dockerfile: Dockerfile.production
      depends_on:
        - builder